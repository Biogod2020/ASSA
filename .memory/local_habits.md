# LOCAL HABITS

- **Efficiency**: Combine `grep_search` and `read_file` calls in parallel to minimize turns.
- **Safety**: Always run `node --check` or relevant syntax checks after modifying executable files.
- **ASSA Context**: Be aware of the L1/L2/L3 memory hierarchy; ensure patterns are distilled from the ledger to `patterns.md`.
- **Subagent-Driven**: Use `generalist` for heavy lifting like deep distillation or cross-file refactoring.
