# ASSA V3.4 (Obsidian-Style Knowledge Graph) Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement a hierarchical, graph-based knowledge management system with lazy-loading and instant promotion.

**Architecture:** 
1.  **Skeleton Graph**: Use `graph.json` as a fast index for metadata and dependencies.
2.  **Node Levels**: Classify rules into L0-L3 for prioritized context injection.
3.  **Recursive Resolver**: Upgrade `beforeAgentHook.js` to perform skeleton-first resolution.
4.  **Curation Logic**: Upgrade `agents/syncer.md` (Promoter) to support versioning, full ingestion, and L0/L1 protection gates.

**Tech Stack:** Node.js, JSON, Markdown.

---

## Chunk 1: Graph Infrastructure & Node Levels

**Files:**
- Modify: `templates/graph.json`
- Modify: `hooks/beforeAgentHook.js` (Type definitions & constants)
- Test: `tests/unit/graph_schema.test.js`

### Task 1: Define Enhanced Graph Schema & Levels

- [ ] **Step 1: Update `templates/graph.json` with version 3.4 structure**

```json
{
  "version": "3.4",
  "last_audit": "",
  "rules": {}
}
```

- [ ] **Step 2: Add Node Level Constants to `hooks/beforeAgentHook.js`**

```javascript
const NODE_LEVELS = {
    L0_CORE: 0,       // SOUL, HANDBOOK
    L1_FOUNDATION: 1, // Language SOTA, Git
    L2_DOMAIN: 2,     // Frameworks (React, etc)
    L3_TRANSIENT: 3   // Project patterns
};
```

- [ ] **Step 3: Commit**

```bash
git add templates/graph.json hooks/beforeAgentHook.js
git commit -m "feat(graph): define node levels and 3.4 schema"
```

---

## Chunk 2: Recursive Skeleton-First Resolver

**Files:**
- Modify: `hooks/beforeAgentHook.js` (Implementation of `resolveGraph`)
- Test: `tests/unit/resolver.test.js`

### Task 2: Implement Skeleton-First Dependency Resolution

- [ ] **Step 1: Write `resolveGraph(seedIds, graph)` in `hooks/beforeAgentHook.js`**

```javascript
function resolveGraph(seedIds, graph) {
    const resolved = {
        meat: new Set(),    // Full content needed
        skeleton: new Set() // Only metadata/rationale needed
    };
    const queue = [...seedIds];
    
    seedIds.forEach(id => resolved.meat.add(id));

    while (queue.length > 0) {
        const currentId = queue.shift();
        const rule = graph.rules[currentId];
        if (rule && rule.depends_on) {
            rule.depends_on.forEach(depId => {
                if (!resolved.meat.has(depId) && !resolved.skeleton.has(depId)) {
                    resolved.skeleton.add(depId);
                    queue.push(depId);
                }
            });
        }
    }
    return resolved;
}
```

- [ ] **Step 2: Update `main` to load Skeleton (Rationale only) for dependencies**

Modify the assembly logic to only inject `rationale` for nodes in the `skeleton` set.

- [ ] **Step 3: Commit**

```bash
git add hooks/beforeAgentHook.js
git commit -m "feat(hook): implement skeleton-first recursive resolution"
```

---

## Chunk 3: Promoter Intelligence (Syncer V3.4)

**Files:**
- Modify: `agents/syncer.md`
- Modify: `hooks/afterToolHook.js` (Add Instant Promotion trigger)

### Task 3: Upgrade Promoter for Versioning & Protection

- [ ] **Step 1: Update `agents/syncer.md` with Versioning (Scheme C) logic**

Add instructions for:
1. Full ingestion of L3.
2. Marking old rules as `deprecated`.
3. Mandatory `ask_user` gate for L0/L1 changes.

- [ ] **Step 2: Implement "Instant Promotion" trigger in `hooks/beforeAgentHook.js` or a separate helper**

Detect strings like "PROMOTED TO GLOBAL" or "ALWAYS APPLY" in agent output (via `AfterAgent` if needed, or by instructing the agent to call Promoter directly). 

*Refinement: Instruct the agent in SOUL.md to call the `generalist` (Promoter) immediately when a pattern is high-value.*

- [ ] **Step 3: Commit**

```bash
git add agents/syncer.md
git commit -m "feat(promoter): implement versioning and L0/L1 protection gates"
```

---

## Chunk 4: Verification & Cleanup

- [ ] **Step 1: Verify E2E flow**
- [ ] **Step 2: Cleanup legacy `index.json` logic if redundant**
- [ ] **Step 3: Final Commit**
